WAAT NEWS CAROUSEL
---------------------------------------

This module includes configuration for .news-carousel component:
- The Views style plugin
- Display Suite display mode

It uses Bootstrap markup and libraries. Be sure to have these lines enabled in
your subtheme:

scripts[] = 'bootstrap-sass/js/bootstrap/carousel.js'
scripts[] = 'bootstrap-sass/js/bootstrap/transition.js'
